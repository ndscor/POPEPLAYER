<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
        android:installLocation="auto">

    <application
        android:name=".VLCApplication"
        android:allowBackup="true"
        android:appCategory="audio"
        android:banner="@mipmap/pope_banner"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/pope_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:logo="@mipmap/pope_launcher"
        android:roundIcon="@mipmap/pope_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.VLC.Transparent"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="q" />
</manifest>
